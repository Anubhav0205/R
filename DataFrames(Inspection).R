data()

data(mtcars)

?mtcars

dim(mtcars)

ncol(mtcars)
nrow(mtcars)

?head()

head(x = mtcars, n = 5)
tail(x = mtcars, n = 5)

str(mtcars)c 

brands = row.names(mtcars)
colnames(mtcars)

summary(mtcars)
